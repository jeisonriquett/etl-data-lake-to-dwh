# etl-data-lake-to-dwh
A robust Python ETL pipeline that extracts raw sales data from a CSV file, transforms it into dimensional tables (customers, products, date), and loads it into a MySQL Data Warehouse using a star schema. Includes validation, error handling, and surrogate key lookups.
